 function notes(state, action) {
    switch(action.type) {
        case ("CHANGE_TEXT"):
            return state
        case ("CHANGE_TITLE"):
            return state
        default:
            return state
    }
}

export default notes