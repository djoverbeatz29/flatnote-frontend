export default function auth(state, action) {
    switch(action.type) {
        case(""):
            return
        case(""):
            return
        default:
            return state
    }
}